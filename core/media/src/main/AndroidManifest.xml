<?xml version="1.0" encoding="utf-8"?>
<!--
  Playback belongs to this module, so its service and the permissions that service
  needs are declared here rather than in the app. The manifest merger folds them
  into the application manifest.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!--
      Required from API 34 (targetSdk >= 34); without it startForeground on the
      media session service throws SecurityException.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application>
        <service
            android:name="com.deox9.musicplayer.player.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>
    </application>

</manifest>
