<?xml version="1.0" encoding="utf-8"?>
<!-- SPDX-License-Identifier: GPL-3.0-or-later -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!--
          exported="true" is required: the launcher is a different process and has to
          be able to reach the provider to render the widget at all.
        -->
        <receiver
            android:name=".NowPlayingWidgetReceiver"
            android:label="@string/widget_now_playing_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/now_playing_widget_info" />
        </receiver>
    </application>
</manifest>
